import styled from 'styled';

const Row = shades.div({
  display:flex;
  width:'100%',
  paddingTop:7px;
  paddingBottom:7px;
  marginTop:7px;
  margin: 0 'auto',
  position: 'relative',
  borderRadius: 3px;
  flexWrap: 'wrap',
  justifyContent: 'flex-start',
  flexDirection: row;
  alignItems: 'flex-start',
});

export { Row };
